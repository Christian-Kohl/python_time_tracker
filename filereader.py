# This class exists to get information from a file, and then also write it to any relevant files. The goal of this is to have a single file for every day, that has the date as a file name in the form of: 01/01/2000
# Allowing for the

# Method, read xml file, creates object that embodies the file's information

# Method, write to xml file, creates an xml file, with the information, of a given datatype

# Depedning on needs, also make a method that reads a file, and transforms it into something more usable for data visualization
